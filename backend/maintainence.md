## Adding

Edit each csv file.

Then use `py ./backend/raw_conversion.py` from the mian file.

## Testing

For testing, `cd docs` then `python -m http.server 8000`

The site will be at [http://localhost:8000/](http://localhost:8000/)